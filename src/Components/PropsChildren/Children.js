function Children(props) {
  return (
    <div className="Children__align">
      <button type="button" class="btn btn-primary">
        {props.name}
      </button>
    </div>
  );
}

export default Children;
